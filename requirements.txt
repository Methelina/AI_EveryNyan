# === Async Core ===
aiohttp>=3.9.0
httpx>=0.27.0

# === Local Analytics DB (for conversation history & RAG context) ===
duckdb>=1.5.0

# === LangChain + Qdrant RAG ===
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-qdrant>=0.1.0
langchain-openai>=0.1.0
qdrant-client[http]>=1.11.0

# === LLM Provider (OpenAI-compatible API) ===
openai>=1.35.0

# === Validation & Config ===
pydantic>=2.8.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# === GUI ===
dearpygui>=1.11.0

# === Utilities ===
structlog>=24.0.0
PyYAML>=6.0.1
tqdm>=4.66.0
spacy>=3.7.0

# === MCP Server (SearXNG) ===
fastmcp>=2.0.0
trafilatura>=2.0.0          # optional (not used in current fetch_url, kept for compatibility)
markdownify>=0.14.0
beautifulsoup4>=4.12.0      # required for HTML cleaning in fetch_url
lxml>=5.0.0                 # recommended for faster parsing with BeautifulSoup

# === MCP Agent Integration ===
langchain-mcp-adapters>=0.1.0
langgraph>=0.4.0